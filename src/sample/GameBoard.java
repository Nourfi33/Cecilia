package sample;

import java.util.ArrayList;
import java.util.Collection;

import javafx.scene.Node;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class GameBoard {

    double x, y, z;
    int w, h;

    ImageView background = new ImageView();
    /*final Image backgroundImage = new Image("file:./resources/background.jpg");


    public GameBoard(){
        background.setImage(backgroundImage);
        background.setTranslateX(-900);
        background.setTranslateY(-500);
        background.setTranslateZ(0);

        this.x = background.getTranslateX();
        this.y = background.getTranslateY();
        this.z = background.getTranslateZ();
        this.w = 1800;
        this.h = 600;
    }

    Collection<Node> getNodes(){
        ArrayList<Node> al = new ArrayList<>();
        al.add(background);
        return al;
    }

*/
}
